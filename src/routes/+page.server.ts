/** @type {import('./$types').Actions} */
export const actions = {
  foo: async (event) => {
    console.log("foo:")
    //console.log(event)
  },
  bar: async (event) => {
    console.log("bar:")
    //console.log(event)
  }
};

